#%Module1.0#####################################################################
##
## ruby/2.1.1  modulefile
##
proc ModulesHelp { } {
        global version

        puts stderr "\n\truby module"
        puts stderr "\t****************************************************"
        puts stderr "\n\t  This module sets up the following environment"
        puts stderr "\t  variables for the gsl libraries:"
        puts stderr "\t      PATH"
        puts stderr "\t      LD_LIBRARY_PATH"
        puts stderr "\n\t  Version $version\n"
        puts stderr "\t****************************************************\n"
}

# prereq intel 

module-whatis	"Set up environment for the MAGMA (Matrix Algebra on GPU and Multicore Architectures)"

# for Tcl script use only
set     version      "3.2.6"

# Make sure no other hpc modulefiles are loaded before loading this module
eval set [ array get env MODULESHOME ]

prepend-path  PATH                 /software/ruby/2.1.1/bin
prepend-path  LD_LIBRARY_PATH      /software/ruby/2.1.1/lib
prepend-path  MANPATH              /software/ruby/2.1.1/share/man

if [ module-info mode display ] {
        ModulesHelp
}

